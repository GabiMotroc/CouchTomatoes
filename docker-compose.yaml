include:
  - path:
    - ./config/docker-compose.yaml
    - ./apps/monitoring/docker-compose.yaml
    - ./apps/media/docker-compose.yaml
    - ./apps/automation/docker-compose.yaml
    - ./apps/management/docker-compose.yaml
    - ./apps/networking/docker-compose.yaml

networks:
  media_network:
    driver: bridge
    
volumes:
  movies:
    driver: local
    driver_opts:
      type: nfs
      o: addr=${NFS_SERVER},nfsvers=4,rw,soft
      device: ":/mnt/main/media/movies"

  shows:
    driver: local
    driver_opts:
      type: nfs
      o: addr=${NFS_SERVER},nfsvers=4,rw,soft
      device: ":/mnt/main/media/tv"

  downloads:
    driver: local
    driver_opts:
      type: nfs
      o: addr=${NFS_SERVER},nfsvers=4,rw,soft
      device: ":/mnt/main/media/downloads"