http:
  routers:
    grafana:
      rule: 'Host(`{{env "GRAFANA_NAME"}}.{{env "DOMAIN"}}`)'
      entryPoints:
        - "websecure"
      service: grafana
      tls:
        certResolver: letsencrypt
        domains:
          - main: '{{env "DOMAIN"}}'
          - sans: '*.{{env "DOMAIN"}}'

    beszel:
      rule: 'Host(`{{env "BESZEL_NAME"}}.{{env "DOMAIN"}}`)'
      entryPoints:
        - "websecure"
      service: beszel
      tls:
        certResolver: letsencrypt
        domains:
          - main: '{{env "DOMAIN"}}'
          - sans: '*.{{env "DOMAIN"}}'

    uptime-kuma:
      rule: 'Host(`{{env "UPTIME_KUMA_NAME"}}.{{env "DOMAIN"}}`)'
      entryPoints:
        - "websecure"
      service: uptime-kuma
      tls:
        certResolver: letsencrypt
        domains:
          - main: '{{env "DOMAIN"}}'
          - sans: '*.{{env "DOMAIN"}}'

  services:
    uptime-kuma:
      loadBalancer:
        servers:
          - url: http://{{env "UPTIME_KUMA_NAME"}}:{{env "UPTIME_KUMA_PORT"}}/

    grafana:
      loadBalancer:
        servers:
          - url: http://{{env "GRAFANA_NAME"}}:{{env "GRAFANA_PORT"}}/

    beszel:
      loadBalancer:
        servers:
          - url: http://{{env "BESZEL_NAME"}}:{{env "BESZEL_PORT"}}/
